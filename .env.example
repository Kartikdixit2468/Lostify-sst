# ==================================================
# LOSTIFY ENVIRONMENT CONFIGURATION
# ==================================================
# SECURITY WARNING: Never commit actual secrets to git!
# Use Replit Secrets or your deployment platform's secret management.

# --------------------------------------------------
# REQUIRED SECRETS (Application will not start without these)
# --------------------------------------------------

# Admin Account Credentials
# These are used to create the first admin user on startup
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=your_secure_password_here
ADMIN_EMAIL=admin@yourdomain.com

# JWT Secret
# Used to sign authentication tokens - must be a long random string
# Generate one at: https://www.random.org/strings/
JWT_SECRET=your_jwt_secret_here

# --------------------------------------------------
# OPTIONAL CONFIGURATION
# --------------------------------------------------

# Server Port (defaults to 5000 if not set)
PORT=5000
