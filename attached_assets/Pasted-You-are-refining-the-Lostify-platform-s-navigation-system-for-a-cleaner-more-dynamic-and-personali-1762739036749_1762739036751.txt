You are refining the Lostify platform’s navigation system for a cleaner, more dynamic, and personalized experience.
Replace the generic profile icon with a dynamic initial-based avatar (first letter of the logged-in user’s name) and update the navbar’s animation style for a fresh, professional look.

👤 1. Dynamic Profile Icon — User Initial Avatar

Goal:
Replace the static profile icon (FaUserCircle) with a circular avatar showing the first letter of the logged-in user’s name (e.g., Deepak → D).

✅ Implementation:

Extract the username or full name from the logged-in user object:

const user = JSON.parse(localStorage.getItem("user"));
const userInitial = user?.name?.[0]?.toUpperCase() || "U";


Replace the icon in the navbar:

<div className="relative">
  <div
    className="h-10 w-10 flex items-center justify-center rounded-full font-semibold text-[#1E2A45] bg-[#F8C538] cursor-pointer hover:opacity-90 transition"
    onClick={() => setMenuOpen(!menuOpen)}
  >
    {userInitial}
  </div>

  {menuOpen && (
    <div className="absolute right-0 mt-2 w-48 bg-[#E7ECEF] dark:bg-[#1E2A45] rounded-lg shadow-lg border border-gray-300 dark:border-gray-700 z-50">
      <button
        className="block w-full text-left px-4 py-2 hover:bg-[#F8C538] hover:text-[#1E2A45]"
        onClick={() => navigate("/settings")}
      >
        ⚙️ Settings
      </button>
      <button
        className="block w-full text-left px-4 py-2 hover:bg-[#F8C538] hover:text-[#1E2A45]"
        onClick={handleLogout}
      >
        🚪 Logout
      </button>
    </div>
  )}
</div>


If Google OAuth provides a profile picture (user.picture), show it instead:

{user?.picture ? (
  <img
    src={user.picture}
    className="h-10 w-10 rounded-full cursor-pointer border-2 border-[#F8C538]"
    onClick={() => setMenuOpen(!menuOpen)}
  />
) : (
  <div
    className="h-10 w-10 flex items-center justify-center rounded-full font-semibold text-[#1E2A45] bg-[#F8C538] cursor-pointer"
    onClick={() => setMenuOpen(!menuOpen)}
  >
    {userInitial}
  </div>
)}


✅ Works seamlessly in both dark and light themes.
✅ Always shows a meaningful avatar — image if available, initial otherwise.

🎬 2. Navbar Animation — Replace Current Animation with Slide + Shadow Reveal

Goal:
Replace the old “fade” animation with a smooth slide-down and shadow reveal effect for a more premium, app-like feel.

✅ Implementation:

Wrap your navbar container with a Framer Motion animation:

import { motion } from "framer-motion";

<motion.nav
  initial={{ y: -60, opacity: 0 }}
  animate={{ y: 0, opacity: 1 }}
  transition={{ duration: 0.5, ease: "easeOut" }}
  className="fixed top-0 w-full bg-[#1E2A45] dark:bg-[#E7ECEF] text-white shadow-md z-50"
>
  {/* Navbar content */}
</motion.nav>


Effect Details:

Navbar slides down from the top on page load.

Subtle shadow appears as it settles (shadow-md).

Smooth easing instead of abrupt pop.

Works perfectly across route transitions.

Optional Enhancement:

Add hover micro-interactions on nav links:

<motion.a
  whileHover={{ scale: 1.05, y: -1 }}
  transition={{ duration: 0.15 }}
  className="hover:text-[#F8C538]"
>
  Browse
</motion.a>


✅ Result: Clean, modern, and performance-friendly animation that doesn’t look “template-made.”

🧭 3. Navbar Layout Adjustments

Keep the layout consistent with previous structure:

| [Lostify Logo] | [Navigation Links] | [Dark Mode Toggle] [User Avatar] |


Ensure:

Profile dropdown closes on outside click or route change.

Avatar remains visible on all screen sizes.

Animation triggers only once on load (not every route change).

🎨 4. Styling & Theme Consistency

Light Mode:

Avatar background: #F8C538

Text: #1E2A45

Navbar background: #E7ECEF

Dark Mode:

Avatar background: #F8C538

Text: #1E2A45

Navbar background: #1E2A45

Font:
Use Manrope 600 for initials for brand consistency.

🧠 5. Summary of Changes
Component	Change	Result
Profile Icon	Replaced with first-letter avatar	Personalized touch for all users
Navbar Animation	New slide + shadow reveal	Smooth, premium entry motion
Profile Dropdown	Retained (Settings + Logout)	Clean, compact UX
Dark/Light Theme	Preserved	Consistent visuals
Logo	Retained in navbar	Brand reinforcement
✅ 6. Quality Checklist
Check	Status
Initial-based avatar displayed	✅
Fallback to profile image	✅
Dropdown functional (Settings + Logout)	✅
Navbar animation replaced	✅
Works on both dark/light modes	✅
Responsive and smooth transitions	✅
🎯 Goal

Deliver a modern, personalized, and visually dynamic navbar where:

The user instantly recognizes their presence via an initial-based avatar.

The navbar entrance feels professional through subtle motion.

All functions (settings, logout, dark mode) remain easily accessible.